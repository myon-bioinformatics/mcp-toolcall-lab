# OpenAI-compatible tool-call mock. Same compose network as LibreChat (service name: openai-mock).
# common.py is the shared stdlib layer (JSONL / call_id / SSE close). Decision
# logic stays in this file — do not COPY the FastMCP server into this image.
FROM python:3.12-slim
WORKDIR /app
COPY src/mcp_toolcall_lab/mock/common.py /app/common.py
COPY demos/openai_toolcall_mock.py /app/openai_toolcall_mock.py
ENV HOST=0.0.0.0
ENV PORT=8090
EXPOSE 8090
CMD ["python", "openai_toolcall_mock.py"]
